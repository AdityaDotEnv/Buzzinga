export type JoinHeroMessage = {
  title: string
  copy: string
}

export type JoinTip = {
  title: string
  copy: string
}

export type RoomPreview = {
  code: string
  title: string
  players: string
  tone: string
}
